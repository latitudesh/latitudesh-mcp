/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { LatitudeshCore } from "../core.js";
import { encodeFormQuery } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  GetServersRequest,
  GetServersRequest$zodSchema,
  GetServersResponse,
  GetServersResponse$zodSchema,
} from "../models/getserversop.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * List all Servers
 *
 * @remarks
 * Returns a list of all servers belonging to the team.
 */
export function serversList(
  client$: LatitudeshCore,
  request?: GetServersRequest | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    GetServersResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    request,
    options,
  ));
}

async function $do(
  client$: LatitudeshCore,
  request?: GetServersRequest | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      GetServersResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => GetServersRequest$zodSchema.optional().parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = null;
  const path$ = pathToFunc("/servers")();
  const query$ = encodeFormQuery({
    "extra_fields[servers]": payload$?.extraFieldsServers,
    "filter[created_at_gte]": payload$?.filterCreatedAtGte,
    "filter[created_at_lte]": payload$?.filterCreatedAtLte,
    "filter[disk]": payload$?.filterDisk,
    "filter[gpu]": payload$?.filterGpu,
    "filter[hostname]": payload$?.filterHostname,
    "filter[label]": payload$?.filterLabel,
    "filter[plan]": payload$?.filterPlan,
    "filter[project]": payload$?.filterProject,
    "filter[ram][eql]": payload$?.filterRamEql,
    "filter[ram][gte]": payload$?.filterRamGte,
    "filter[ram][lte]": payload$?.filterRamLte,
    "filter[region]": payload$?.filterRegion,
    "filter[status]": payload$?.filterStatus,
    "filter[tags]": payload$?.filterTags,
    "page[number]": payload$?.pageNumber,
    "page[size]": payload$?.pageSize,
  });

  const headers$ = new Headers(compactMap({
    Accept: "application/vnd.api+json",
  }));
  const securityInput = await extractSecurity(client$._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client$._options,
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "get-servers",
    oAuth2Scopes: null,
    resolvedSecurity: requestSecurity,
    securitySource: client$._options.security,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    query: query$,
    body: body$,
    userAgent: client$._options.userAgent,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    GetServersResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(200, GetServersResponse$zodSchema, {
      ctype: "application/vnd.api+json",
      key: "servers",
    }),
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: "complete", request: req$, response }];
}
