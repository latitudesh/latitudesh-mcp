/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversRunAction } from "../../funcs/serversRunAction.js";
import { CreateServerActionRequest$zodSchema } from "../../models/createserveractionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateServerActionRequest$zodSchema,
};

export const tool$serversRunAction: ToolDefinition<typeof args> = {
  name: "servers-run-action",
  description: `Run Server Action

Performs an action on a given server:
- \`power_on\`
- \`power_off\`
- \`reboot\`
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversRunAction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
