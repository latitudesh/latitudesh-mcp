/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { firewallsGet } from "../../funcs/firewallsGet.js";
import { GetFirewallRequest$zodSchema } from "../../models/getfirewallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFirewallRequest$zodSchema,
};

export const tool$firewallsGet: ToolDefinition<typeof args> = {
  name: "firewalls-get",
  description: `Retrieve Firewall

Retrieve a firewall`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await firewallsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
