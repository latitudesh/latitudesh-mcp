/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStorageRetrieveVolume } from "../../funcs/blockStorageRetrieveVolume.js";
import { RetrieveVolumeRequest$zodSchema } from "../../models/retrievevolumeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RetrieveVolumeRequest$zodSchema,
};

export const tool$blockStorageRetrieveVolume: ToolDefinition<typeof args> = {
  name: "block-storage-retrieve-volume",
  description: `Retrieve volume

Shows details of a specific volume.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await blockStorageRetrieveVolume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
