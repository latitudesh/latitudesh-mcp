/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sshKeysGet } from "../../funcs/sshKeysGet.js";
import { GetProjectSshKeyRequest$zodSchema } from "../../models/getprojectsshkeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetProjectSshKeyRequest$zodSchema,
};

export const tool$sshKeysGet: ToolDefinition<typeof args> = {
  name: "SSH-keys-get",
  description: `Retrieve a Project SSH Key

List all SSH Keys in the project. These keys can be used to access servers after deploy and reinstall actions.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sshKeysGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
