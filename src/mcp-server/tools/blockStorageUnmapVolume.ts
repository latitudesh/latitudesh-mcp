/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStorageUnmapVolume } from "../../funcs/blockStorageUnmapVolume.js";
import { UnmapVolumeRequest$zodSchema } from "../../models/unmapvolumeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UnmapVolumeRequest$zodSchema,
};

export const tool$blockStorageUnmapVolume: ToolDefinition<typeof args> = {
  name: "block-storage-unmap-volume",
  description: `Unmap volume

Unmaps a high performance volume from the server it is currently mapped to.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await blockStorageUnmapVolume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
