/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversUpdateDeployConfig } from "../../funcs/serversUpdateDeployConfig.js";
import { UpdateServerDeployConfigRequest$zodSchema } from "../../models/updateserverdeployconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateServerDeployConfigRequest$zodSchema,
};

export const tool$serversUpdateDeployConfig: ToolDefinition<typeof args> = {
  name: "servers-update-deploy-config",
  description: `Update Deploy Config`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversUpdateDeployConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
