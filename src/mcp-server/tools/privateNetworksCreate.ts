/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { privateNetworksCreate } from "../../funcs/privateNetworksCreate.js";
import { CreateVirtualNetworkRequest$zodSchema } from "../../models/createvirtualnetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateVirtualNetworkRequest$zodSchema,
};

export const tool$privateNetworksCreate: ToolDefinition<typeof args> = {
  name: "private-networks-create",
  description: `Create a Virtual Network

Creates a new Virtual Network.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await privateNetworksCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
