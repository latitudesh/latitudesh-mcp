/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { firewallsDeleteAssignment } from "../../funcs/firewallsDeleteAssignment.js";
import { DeleteFirewallAssignmentRequest$zodSchema } from "../../models/deletefirewallassignmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteFirewallAssignmentRequest$zodSchema,
};

export const tool$firewallsDeleteAssignment: ToolDefinition<typeof args> = {
  name: "firewalls-delete-assignment",
  description: `Delete assignment

Removes a server from a firewall by its ID.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await firewallsDeleteAssignment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
