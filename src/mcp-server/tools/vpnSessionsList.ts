/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vpnSessionsList } from "../../funcs/vpnSessionsList.js";
import { GetVpnSessionsRequest$zodSchema } from "../../models/getvpnsessionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetVpnSessionsRequest$zodSchema.optional(),
};

export const tool$vpnSessionsList: ToolDefinition<typeof args> = {
  name: "vpn-sessions-list",
  description: `List all Active VPN Sessions`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await vpnSessionsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
