/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kubernetesClustersGetKubernetesClusterKubeconfig } from "../../funcs/kubernetesClustersGetKubernetesClusterKubeconfig.js";
import { GetKubernetesClusterKubeconfigRequest$zodSchema } from "../../models/getkubernetesclusterkubeconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetKubernetesClusterKubeconfigRequest$zodSchema,
};

export const tool$kubernetesClustersGetKubernetesClusterKubeconfig:
  ToolDefinition<typeof args> = {
    name: "kubernetes-clusters-get-kubernetes-cluster-fbb",
    description: `Get Kubernetes Cluster Kubeconfig

Retrieves the kubeconfig file for a Kubernetes cluster. The kubeconfig is only available once the cluster is fully provisioned.
`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await kubernetesClustersGetKubernetesClusterKubeconfig(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
