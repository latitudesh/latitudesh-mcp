/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { elasticIpsCreateElasticIp } from "../../funcs/elasticIpsCreateElasticIp.js";
import { CreateElasticIp$zodSchema } from "../../models/createelasticip.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateElasticIp$zodSchema,
};

export const tool$elasticIpsCreateElasticIp: ToolDefinition<typeof args> = {
  name: "elastic-ips-create-elastic-ip",
  description: `Create an Elastic IP

Creates a new Elastic IP and assigns it to the specified server. The IP is provisioned asynchronously—the response will show status \`configuring\` and the \`id\` will be \`null\` until provisioning completes. Currently only IPv4 /32 addresses in routed mode are supported.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await elasticIpsCreateElasticIp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
