/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { privateNetworksAssign } from "../../funcs/privateNetworksAssign.js";
import { AssignServerVirtualNetworkRequest$zodSchema } from "../../models/assignservervirtualnetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AssignServerVirtualNetworkRequest$zodSchema,
};

export const tool$privateNetworksAssign: ToolDefinition<typeof args> = {
  name: "private-networks-assign",
  description: `Assign Virtual network`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await privateNetworksAssign(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
