/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { elasticIpsGetElasticIpBgpSession } from "../../funcs/elasticIpsGetElasticIpBgpSession.js";
import { GetElasticIpBgpSessionRequest$zodSchema } from "../../models/getelasticipbgpsessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetElasticIpBgpSessionRequest$zodSchema,
};

export const tool$elasticIpsGetElasticIpBgpSession: ToolDefinition<
  typeof args
> = {
  name: "elastic-ips-get-elastic-ip-bgp-session",
  description: `Retrieve a BGP session

Retrieve a BGP session announcing an elastic IP`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await elasticIpsGetElasticIpBgpSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
