/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStorageDeleteVolume } from "../../funcs/blockStorageDeleteVolume.js";
import { DeleteVolumeRequest$zodSchema } from "../../models/deletevolumeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteVolumeRequest$zodSchema,
};

export const tool$blockStorageDeleteVolume: ToolDefinition<typeof args> = {
  name: "block-storage-delete-volume",
  description: `Delete volume

Allows you to remove a volume from a project.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await blockStorageDeleteVolume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
