/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachineRestoresCreate } from "../../funcs/virtualMachineRestoresCreate.js";
import { VirtualMachineRestorePayload$zodSchema } from "../../models/virtualmachinerestorepayload.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VirtualMachineRestorePayload$zodSchema,
};

export const tool$virtualMachineRestoresCreate: ToolDefinition<typeof args> = {
  name: "virtual-machine-restores-create",
  description: `Create VM restore (flat)

Restores the backup referenced in the body into a new Virtual Machine. Optionally accepts a \`name\` for the restored VM and a target \`site\` slug to restore into another region.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await virtualMachineRestoresCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
