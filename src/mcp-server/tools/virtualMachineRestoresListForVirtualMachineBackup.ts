/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachineRestoresListForVirtualMachineBackup } from "../../funcs/virtualMachineRestoresListForVirtualMachineBackup.js";
import { ListVirtualMachineScopedRestoresRequest$zodSchema } from "../../models/listvirtualmachinescopedrestoresop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListVirtualMachineScopedRestoresRequest$zodSchema,
};

export const tool$virtualMachineRestoresListForVirtualMachineBackup:
  ToolDefinition<typeof args> = {
    name: "virtual-machine-restores-list-for-virtual-6f5",
    description: `List a backup's restores

Lists the restores created from the given backup.
`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await virtualMachineRestoresListForVirtualMachineBackup(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
