/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kubernetesClustersListKubernetesClusters } from "../../funcs/kubernetesClustersListKubernetesClusters.js";
import { ListKubernetesClustersRequest$zodSchema } from "../../models/listkubernetesclustersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListKubernetesClustersRequest$zodSchema,
};

export const tool$kubernetesClustersListKubernetesClusters: ToolDefinition<
  typeof args
> = {
  name: "kubernetes-clusters-list-kubernetes-clusters",
  description: `List Kubernetes Clusters

Lists all Kubernetes clusters for a project.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kubernetesClustersListKubernetesClusters(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
