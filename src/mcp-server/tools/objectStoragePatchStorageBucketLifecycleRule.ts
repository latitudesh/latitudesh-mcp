/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStoragePatchStorageBucketLifecycleRule } from "../../funcs/objectStoragePatchStorageBucketLifecycleRule.js";
import { PatchStorageBucketLifecycleRuleRequest$zodSchema } from "../../models/patchstoragebucketlifecycleruleop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PatchStorageBucketLifecycleRuleRequest$zodSchema,
};

export const tool$objectStoragePatchStorageBucketLifecycleRule: ToolDefinition<
  typeof args
> = {
  name: "object-storage-patch-storage-bucket-lifecycle-rule",
  description: `Update lifecycle rule

Updates an existing lifecycle rule for an object storage bucket.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStoragePatchStorageBucketLifecycleRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
