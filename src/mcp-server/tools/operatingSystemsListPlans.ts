/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { operatingSystemsListPlans } from "../../funcs/operatingSystemsListPlans.js";
import { GetPlansOperatingSystemRequest$zodSchema } from "../../models/getplansoperatingsystemop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPlansOperatingSystemRequest$zodSchema.optional(),
};

export const tool$operatingSystemsListPlans: ToolDefinition<typeof args> = {
  name: "operating-systems-list-plans",
  description: `List all operating systems available

Lists all operating systems available to deploy and reinstall.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await operatingSystemsListPlans(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
