/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamMembersPostTeamMembers } from "../../funcs/teamMembersPostTeamMembers.js";
import { PostTeamMembersRequest$zodSchema } from "../../models/postteammembersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostTeamMembersRequest$zodSchema,
};

export const tool$teamMembersPostTeamMembers: ToolDefinition<typeof args> = {
  name: "team-members-post-team-members",
  description: `Add a Team Member`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamMembersPostTeamMembers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
