/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageDeleteStorageBucketLifecycleRule } from "../../funcs/objectStorageDeleteStorageBucketLifecycleRule.js";
import { DeleteStorageBucketLifecycleRuleRequest$zodSchema } from "../../models/deletestoragebucketlifecycleruleop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteStorageBucketLifecycleRuleRequest$zodSchema,
};

export const tool$objectStorageDeleteStorageBucketLifecycleRule: ToolDefinition<
  typeof args
> = {
  name: "object-storage-delete-storage-bucket-lifecycle-rule",
  description: `Delete lifecycle rule

Deletes a lifecycle rule from an object storage bucket.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStorageDeleteStorageBucketLifecycleRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
