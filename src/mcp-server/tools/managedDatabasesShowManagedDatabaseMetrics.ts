/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { managedDatabasesShowManagedDatabaseMetrics } from "../../funcs/managedDatabasesShowManagedDatabaseMetrics.js";
import { ShowManagedDatabaseMetricsRequest$zodSchema } from "../../models/showmanageddatabasemetricsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ShowManagedDatabaseMetricsRequest$zodSchema,
};

export const tool$managedDatabasesShowManagedDatabaseMetrics: ToolDefinition<
  typeof args
> = {
  name: "managed-databases-show-managed-database-metrics",
  description: `Show managed database metrics`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await managedDatabasesShowManagedDatabaseMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
