/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { baselinesPreviewGetBaseline } from "../../funcs/baselinesPreviewGetBaseline.js";
import { GetBaselineRequest$zodSchema } from "../../models/getbaselineop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetBaselineRequest$zodSchema,
};

export const tool$baselinesPreviewGetBaseline: ToolDefinition<typeof args> = {
  name: "baselines-preview-get-baseline",
  description: `Retrieve baseline

**Preview.** Available to teams with the \`baselines_api\` feature flag.

Retrieve a single baseline.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await baselinesPreviewGetBaseline(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
