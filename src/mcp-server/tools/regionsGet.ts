/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { regionsGet } from "../../funcs/regionsGet.js";
import { GetRegionsRequest$zodSchema } from "../../models/getregionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetRegionsRequest$zodSchema.optional(),
};

export const tool$regionsGet: ToolDefinition<typeof args> = {
  name: "regions-get",
  description: `List all Regions

Lists all [available locations](https://latitude.sh/locations). For server availability by location, please see the [Plans API](/reference/get-plans).

`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await regionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
