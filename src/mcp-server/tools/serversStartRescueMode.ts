/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversStartRescueMode } from "../../funcs/serversStartRescueMode.js";
import { ServerStartRescueModeRequest$zodSchema } from "../../models/serverstartrescuemodeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ServerStartRescueModeRequest$zodSchema,
};

export const tool$serversStartRescueMode: ToolDefinition<typeof args> = {
  name: "servers-start-rescue-mode",
  description: `Puts a Server in rescue mode

Starts rescue mode on a given server.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversStartRescueMode(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
