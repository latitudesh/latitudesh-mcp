/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trafficGetQuota } from "../../funcs/trafficGetQuota.js";
import { GetTrafficQuotaRequest$zodSchema } from "../../models/gettrafficquotaop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTrafficQuotaRequest$zodSchema.optional(),
};

export const tool$trafficGetQuota: ToolDefinition<typeof args> = {
  name: "traffic-get-quota",
  description: `Retrieve Traffic Quota`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trafficGetQuota(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
