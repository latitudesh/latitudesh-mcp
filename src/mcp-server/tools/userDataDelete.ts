/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userDataDelete } from "../../funcs/userDataDelete.js";
import { DeleteUserDataRequest$zodSchema } from "../../models/deleteuserdataop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteUserDataRequest$zodSchema,
};

export const tool$userDataDelete: ToolDefinition<typeof args> = {
  name: "user-data-delete",
  description: `Delete an User Data`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userDataDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
