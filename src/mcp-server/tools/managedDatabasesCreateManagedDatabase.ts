/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { managedDatabasesCreateManagedDatabase } from "../../funcs/managedDatabasesCreateManagedDatabase.js";
import { ManagedDatabasePayload$zodSchema } from "../../models/manageddatabasepayload.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ManagedDatabasePayload$zodSchema,
};

export const tool$managedDatabasesCreateManagedDatabase: ToolDefinition<
  typeof args
> = {
  name: "managed-databases-create-managed-database",
  description: `Create a managed database`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await managedDatabasesCreateManagedDatabase(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
