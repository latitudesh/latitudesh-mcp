/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lksDeleteLKSNodePool } from "../../funcs/lksDeleteLKSNodePool.js";
import { DeleteLksNodePoolRequest$zodSchema } from "../../models/deletelksnodepoolop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteLksNodePoolRequest$zodSchema,
};

export const tool$lksDeleteLKSNodePool: ToolDefinition<typeof args> = {
  name: "lks-delete-lks-node-pool",
  description: `Delete a node pool

Marks the node pool for deletion. The call returns as soon as the tombstone is written; the platform then drains and releases the nodes asynchronously.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await lksDeleteLKSNodePool(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
