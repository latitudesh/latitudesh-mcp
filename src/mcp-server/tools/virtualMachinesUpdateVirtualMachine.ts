/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachinesUpdateVirtualMachine } from "../../funcs/virtualMachinesUpdateVirtualMachine.js";
import { UpdateVirtualMachineRequest$zodSchema } from "../../models/updatevirtualmachineop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateVirtualMachineRequest$zodSchema,
};

export const tool$virtualMachinesUpdateVirtualMachine: ToolDefinition<
  typeof args
> = {
  name: "virtual-machines-update-virtual-machine",
  description: `Update VM

Updates a Virtual Machine's display name (hostname).
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await virtualMachinesUpdateVirtualMachine(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
