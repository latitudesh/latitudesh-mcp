/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversStartOutOfBandConnection } from "../../funcs/serversStartOutOfBandConnection.js";
import { CreateServerOutOfBandRequest$zodSchema } from "../../models/createserveroutofbandop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateServerOutOfBandRequest$zodSchema,
};

export const tool$serversStartOutOfBandConnection: ToolDefinition<typeof args> =
  {
    name: "servers-start-out-of-band-connection",
    description: `Start Out of Band Connection`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await serversStartOutOfBandConnection(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
