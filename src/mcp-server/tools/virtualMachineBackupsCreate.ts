/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachineBackupsCreate } from "../../funcs/virtualMachineBackupsCreate.js";
import { VirtualMachineBackupPayload$zodSchema } from "../../models/virtualmachinebackuppayload.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VirtualMachineBackupPayload$zodSchema,
};

export const tool$virtualMachineBackupsCreate: ToolDefinition<typeof args> = {
  name: "virtual-machine-backups-create",
  description: `Create VM backup (top-level)

Triggers a backup of the Virtual Machine referenced in the body.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await virtualMachineBackupsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
