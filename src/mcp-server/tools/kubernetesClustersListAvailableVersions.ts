/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kubernetesClustersListAvailableVersions } from "../../funcs/kubernetesClustersListAvailableVersions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$kubernetesClustersListAvailableVersions: ToolDefinition = {
  name: "kubernetes-clusters-list-available-versions",
  description: `List Available Kubernetes Versions

Returns the list of available Kubernetes versions for cluster creation and upgrades. Versions are sourced from the RKE2 release channels and cached for 24 hours.

Each version object includes:
- \`latest\`: The full version string (e.g., \`v1.35.3+rke2r1\`)
- \`minor\`: The minor version number (e.g., \`1.35\`)

The API returns the latest 5 supported minor versions. When upgrading clusters, you can only upgrade one minor version at a time (e.g., from 1.34 to 1.35).
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await kubernetesClustersListAvailableVersions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
