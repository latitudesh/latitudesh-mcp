/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachinesCreateVirtualMachineAction } from "../../funcs/virtualMachinesCreateVirtualMachineAction.js";
import { CreateVirtualMachineActionRequest$zodSchema } from "../../models/createvirtualmachineactionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateVirtualMachineActionRequest$zodSchema,
};

export const tool$virtualMachinesCreateVirtualMachineAction: ToolDefinition<
  typeof args
> = {
  name: "virtual-machines-create-virtual-machine-action",
  description: `Run Virtual Machine Action

Performs a power action on a given virtual machine:
- \`power_on\` - Starts the virtual machine
- \`power_off\` - Stops the virtual machine
- \`reboot\` - Restarts the virtual machine
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await virtualMachinesCreateVirtualMachineAction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
