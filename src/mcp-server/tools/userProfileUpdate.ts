/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userProfileUpdate } from "../../funcs/userProfileUpdate.js";
import { PatchUserProfileRequest$zodSchema } from "../../models/patchuserprofileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PatchUserProfileRequest$zodSchema,
};

export const tool$userProfileUpdate: ToolDefinition<typeof args> = {
  name: "user-profile-update",
  description: `Update User Profile

Update the current user profile
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userProfileUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
