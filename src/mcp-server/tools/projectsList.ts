/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsList } from "../../funcs/projectsList.js";
import { GetProjectsRequest$zodSchema } from "../../models/getprojectsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetProjectsRequest$zodSchema.optional(),
};

export const tool$projectsList: ToolDefinition<typeof args> = {
  name: "projects-list",
  description: `List all Projects

Returns a list of all projects for the current team
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
