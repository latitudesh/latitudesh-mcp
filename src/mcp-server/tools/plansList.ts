/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { plansList } from "../../funcs/plansList.js";
import { GetPlansRequest$zodSchema } from "../../models/getplansop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPlansRequest$zodSchema.optional(),
};

export const tool$plansList: ToolDefinition<typeof args> = {
  name: "plans-list",
  description: `List plans

Lists all plans. Availability by region is included in \`attributes.regions.locations.available[*]\` node for a given plan.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await plansList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
