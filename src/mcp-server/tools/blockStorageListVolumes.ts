/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStorageListVolumes } from "../../funcs/blockStorageListVolumes.js";
import { ListVolumesRequest$zodSchema } from "../../models/listvolumesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListVolumesRequest$zodSchema.optional(),
};

export const tool$blockStorageListVolumes: ToolDefinition<typeof args> = {
  name: "block-storage-list-volumes",
  description: `List volumes

Lists all the volumes from a team.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await blockStorageListVolumes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
