/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStorageGetStorageVolume } from "../../funcs/blockStorageGetStorageVolume.js";
import { GetStorageVolumeRequest$zodSchema } from "../../models/getstoragevolumeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetStorageVolumeRequest$zodSchema,
};

export const tool$blockStorageGetStorageVolume: ToolDefinition<typeof args> = {
  name: "block-storage-get-storage-volume",
  description: `Retrieve volume

Shows details of a specific volume.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await blockStorageGetStorageVolume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
