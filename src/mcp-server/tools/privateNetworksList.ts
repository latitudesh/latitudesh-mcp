/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { privateNetworksList } from "../../funcs/privateNetworksList.js";
import { GetVirtualNetworksRequest$zodSchema } from "../../models/getvirtualnetworksop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetVirtualNetworksRequest$zodSchema.optional(),
};

export const tool$privateNetworksList: ToolDefinition<typeof args> = {
  name: "private-networks-list",
  description: `List all Virtual Networks

Lists virtual networks assigned to a project
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await privateNetworksList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
