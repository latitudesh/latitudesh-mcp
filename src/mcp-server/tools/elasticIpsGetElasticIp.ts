/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { elasticIpsGetElasticIp } from "../../funcs/elasticIpsGetElasticIp.js";
import { GetElasticIpRequest$zodSchema } from "../../models/getelasticipop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetElasticIpRequest$zodSchema,
};

export const tool$elasticIpsGetElasticIp: ToolDefinition<typeof args> = {
  name: "elastic-ips-get-elastic-ip",
  description: `Retrieve an Elastic IP

Returns a single Elastic IP by its ID.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await elasticIpsGetElasticIp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
