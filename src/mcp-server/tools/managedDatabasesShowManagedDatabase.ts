/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { managedDatabasesShowManagedDatabase } from "../../funcs/managedDatabasesShowManagedDatabase.js";
import { ShowManagedDatabaseRequest$zodSchema } from "../../models/showmanageddatabaseop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ShowManagedDatabaseRequest$zodSchema,
};

export const tool$managedDatabasesShowManagedDatabase: ToolDefinition<
  typeof args
> = {
  name: "managed-databases-show-managed-database",
  description: `Show a managed database`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await managedDatabasesShowManagedDatabase(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
