/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachinesDelete } from "../../funcs/virtualMachinesDelete.js";
import { DestroyVirtualMachineRequest$zodSchema } from "../../models/destroyvirtualmachineop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DestroyVirtualMachineRequest$zodSchema,
};

export const tool$virtualMachinesDelete: ToolDefinition<typeof args> = {
  name: "virtual-machines-delete",
  description: `Destroy a Virtual Machine

Destroys a Virtual Machine.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await virtualMachinesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
