/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { privateNetworksUpdate } from "../../funcs/privateNetworksUpdate.js";
import { UpdateVirtualNetworkRequest$zodSchema } from "../../models/updatevirtualnetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateVirtualNetworkRequest$zodSchema,
};

export const tool$privateNetworksUpdate: ToolDefinition<typeof args> = {
  name: "private-networks-update",
  description: `Update a Virtual Network

Update a Virtual Network.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await privateNetworksUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
