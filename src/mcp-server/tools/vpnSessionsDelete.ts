/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vpnSessionsDelete } from "../../funcs/vpnSessionsDelete.js";
import { DeleteVpnSessionRequest$zodSchema } from "../../models/deletevpnsessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteVpnSessionRequest$zodSchema,
};

export const tool$vpnSessionsDelete: ToolDefinition<typeof args> = {
  name: "vpn-sessions-delete",
  description: `Delete a VPN Session

Deletes an existing VPN Session.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await vpnSessionsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
