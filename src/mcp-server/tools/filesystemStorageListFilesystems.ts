/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filesystemStorageListFilesystems } from "../../funcs/filesystemStorageListFilesystems.js";
import { GetStorageFilesystemsRequest$zodSchema } from "../../models/getstoragefilesystemsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetStorageFilesystemsRequest$zodSchema.optional(),
};

export const tool$filesystemStorageListFilesystems: ToolDefinition<
  typeof args
> = {
  name: "filesystem-storage-list-filesystems",
  description: `List filesystems

Lists all the filesystems from a team.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await filesystemStorageListFilesystems(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
