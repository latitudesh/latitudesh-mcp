/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { managedDatabasesListManagedDatabaseBackups } from "../../funcs/managedDatabasesListManagedDatabaseBackups.js";
import { ListManagedDatabaseBackupsRequest$zodSchema } from "../../models/listmanageddatabasebackupsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListManagedDatabaseBackupsRequest$zodSchema,
};

export const tool$managedDatabasesListManagedDatabaseBackups: ToolDefinition<
  typeof args
> = {
  name: "managed-databases-list-managed-database-backups",
  description: `List managed database backups`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await managedDatabasesListManagedDatabaseBackups(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
