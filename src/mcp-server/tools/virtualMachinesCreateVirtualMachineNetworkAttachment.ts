/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachinesCreateVirtualMachineNetworkAttachment } from "../../funcs/virtualMachinesCreateVirtualMachineNetworkAttachment.js";
import { CreateVirtualMachineNetworkAttachmentRequest$zodSchema } from "../../models/createvirtualmachinenetworkattachmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateVirtualMachineNetworkAttachmentRequest$zodSchema,
};

export const tool$virtualMachinesCreateVirtualMachineNetworkAttachment:
  ToolDefinition<typeof args> = {
    name: "virtual-machines-create-virtual-machine-network-3b8",
    description: `Attach a network to a VM

Attaches a virtual network (VLAN) to a Virtual Machine. Work runs asynchronously and returns 202 Accepted.
`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await virtualMachinesCreateVirtualMachineNetworkAttachment(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
