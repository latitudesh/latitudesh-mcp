/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { elasticIpsCreateElasticIpBgpSession } from "../../funcs/elasticIpsCreateElasticIpBgpSession.js";
import { CreateElasticIpBgpSessionRequest$zodSchema } from "../../models/createelasticipbgpsessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateElasticIpBgpSessionRequest$zodSchema,
};

export const tool$elasticIpsCreateElasticIpBgpSession: ToolDefinition<
  typeof args
> = {
  name: "elastic-ips-create-elastic-ip-bgp-session",
  description: `Create a BGP session

Announce an elastic IP from a server over BGP`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await elasticIpsCreateElasticIpBgpSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
