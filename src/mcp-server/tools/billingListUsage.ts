/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { billingListUsage } from "../../funcs/billingListUsage.js";
import { GetBillingUsageRequest$zodSchema } from "../../models/getbillingusageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetBillingUsageRequest$zodSchema,
};

export const tool$billingListUsage: ToolDefinition<typeof args> = {
  name: "billing-list-usage",
  description: `List Billing Usage

Returns the billing usage of a project
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await billingListUsage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
