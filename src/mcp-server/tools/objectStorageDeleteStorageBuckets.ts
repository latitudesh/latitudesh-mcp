/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageDeleteStorageBuckets } from "../../funcs/objectStorageDeleteStorageBuckets.js";
import { DeleteStorageBucketsRequest$zodSchema } from "../../models/deletestoragebucketsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteStorageBucketsRequest$zodSchema,
};

export const tool$objectStorageDeleteStorageBuckets: ToolDefinition<
  typeof args
> = {
  name: "object-storage-delete-storage-buckets",
  description: `Delete bucket

Allows you to remove an object storage from a project.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStorageDeleteStorageBuckets(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
