/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageGetStorageBucketMetrics } from "../../funcs/objectStorageGetStorageBucketMetrics.js";
import { GetStorageBucketMetricsRequest$zodSchema } from "../../models/getstoragebucketmetricsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetStorageBucketMetricsRequest$zodSchema,
};

export const tool$objectStorageGetStorageBucketMetrics: ToolDefinition<
  typeof args
> = {
  name: "object-storage-get-storage-bucket-metrics",
  description: `Retrieve bucket metrics

Retrieves usage metrics for a specific object storage bucket, including storage consumption and estimated cost for the current billing period.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStorageGetStorageBucketMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
