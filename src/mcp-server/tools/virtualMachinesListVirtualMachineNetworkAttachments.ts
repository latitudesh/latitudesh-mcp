/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachinesListVirtualMachineNetworkAttachments } from "../../funcs/virtualMachinesListVirtualMachineNetworkAttachments.js";
import { ListVirtualMachineNetworkAttachmentsRequest$zodSchema } from "../../models/listvirtualmachinenetworkattachmentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListVirtualMachineNetworkAttachmentsRequest$zodSchema,
};

export const tool$virtualMachinesListVirtualMachineNetworkAttachments:
  ToolDefinition<typeof args> = {
    name: "virtual-machines-list-network-attachments",
    description: `List VM network attachments

Lists the secondary network attachments currently configured for a Virtual Machine.
`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await virtualMachinesListVirtualMachineNetworkAttachments(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
