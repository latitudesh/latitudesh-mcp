/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversUnscheduleDeletion } from "../../funcs/serversUnscheduleDeletion.js";
import { ServerUnscheduleDeletionRequest$zodSchema } from "../../models/serverunscheduledeletionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ServerUnscheduleDeletionRequest$zodSchema,
};

export const tool$serversUnscheduleDeletion: ToolDefinition<typeof args> = {
  name: "servers-unschedule-deletion",
  description: `Unschedule the server deletion

Unschedules the server removal at the end of the billing cycle.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversUnscheduleDeletion(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
