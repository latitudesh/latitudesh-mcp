/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publicNetworksDestroyPublicNetwork } from "../../funcs/publicNetworksDestroyPublicNetwork.js";
import { DestroyPublicNetworkRequest$zodSchema } from "../../models/destroypublicnetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DestroyPublicNetworkRequest$zodSchema,
};

export const tool$publicNetworksDestroyPublicNetwork: ToolDefinition<
  typeof args
> = {
  name: "public-networks-destroy-public-network",
  description: `Delete a network

Delete a customer network. Only allowed while the network has no IPs in use.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await publicNetworksDestroyPublicNetwork(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
