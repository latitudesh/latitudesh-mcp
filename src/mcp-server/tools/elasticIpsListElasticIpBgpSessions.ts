/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { elasticIpsListElasticIpBgpSessions } from "../../funcs/elasticIpsListElasticIpBgpSessions.js";
import { ListElasticIpBgpSessionsRequest$zodSchema } from "../../models/listelasticipbgpsessionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListElasticIpBgpSessionsRequest$zodSchema,
};

export const tool$elasticIpsListElasticIpBgpSessions: ToolDefinition<
  typeof args
> = {
  name: "elastic-ips-list-elastic-ip-bgp-sessions",
  description: `List BGP sessions

List the BGP sessions announcing an elastic IP`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await elasticIpsListElasticIpBgpSessions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
