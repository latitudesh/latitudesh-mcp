/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsUpdate } from "../../funcs/teamsUpdate.js";
import { PatchCurrentTeamRequest$zodSchema } from "../../models/patchcurrentteamop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PatchCurrentTeamRequest$zodSchema,
};

export const tool$teamsUpdate: ToolDefinition<typeof args> = {
  name: "teams-update",
  description: `Update a team`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
