/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageGetStorageAccessKeys } from "../../funcs/objectStorageGetStorageAccessKeys.js";
import { GetStorageAccessKeysRequest$zodSchema } from "../../models/getstorageaccesskeysop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetStorageAccessKeysRequest$zodSchema,
};

export const tool$objectStorageGetStorageAccessKeys: ToolDefinition<
  typeof args
> = {
  name: "object-storage-get-storage-access-keys",
  description: `List access keys

Lists object storage access keys for a project, grouped by storage class. Secrets are never returned by this endpoint.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStorageGetStorageAccessKeys(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
