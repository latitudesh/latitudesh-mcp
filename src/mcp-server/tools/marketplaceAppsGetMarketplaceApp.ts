/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { marketplaceAppsGetMarketplaceApp } from "../../funcs/marketplaceAppsGetMarketplaceApp.js";
import { GetMarketplaceAppRequest$zodSchema } from "../../models/getmarketplaceappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMarketplaceAppRequest$zodSchema,
};

export const tool$marketplaceAppsGetMarketplaceApp: ToolDefinition<
  typeof args
> = {
  name: "marketplace-apps-get-marketplace-app",
  description: `Retrieve a marketplace app

Retrieve a single marketplace app by ID or slug. Only published apps are visible. Requires the \`marketplace_apps\` feature to be enabled for the team.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await marketplaceAppsGetMarketplaceApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
