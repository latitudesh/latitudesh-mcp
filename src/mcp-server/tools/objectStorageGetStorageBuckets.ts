/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageGetStorageBuckets } from "../../funcs/objectStorageGetStorageBuckets.js";
import { GetStorageBucketsRequest$zodSchema } from "../../models/getstoragebucketsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetStorageBucketsRequest$zodSchema.optional(),
};

export const tool$objectStorageGetStorageBuckets: ToolDefinition<typeof args> =
  {
    name: "object-storage-get-storage-buckets",
    description: `List object storages

Lists all object storages from a team.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await objectStorageGetStorageBuckets(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
