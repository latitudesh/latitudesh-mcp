/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vpnSessionsRefreshPassword } from "../../funcs/vpnSessionsRefreshPassword.js";
import { PutVpnSessionRequest$zodSchema } from "../../models/putvpnsessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutVpnSessionRequest$zodSchema,
};

export const tool$vpnSessionsRefreshPassword: ToolDefinition<typeof args> = {
  name: "vpn-sessions-refresh-password",
  description: `Refresh VPN session

Refreshing an existing VPN Session will create new credentials for that session
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vpnSessionsRefreshPassword(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
