/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { elasticIpsDeleteElasticIp } from "../../funcs/elasticIpsDeleteElasticIp.js";
import { DeleteElasticIpRequest$zodSchema } from "../../models/deleteelasticipop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteElasticIpRequest$zodSchema,
};

export const tool$elasticIpsDeleteElasticIp: ToolDefinition<typeof args> = {
  name: "elastic-ips-delete-elastic-ip",
  description: `Release an Elastic IP

Releases an Elastic IP, returning it to the available pool. The IP will transition to \`releasing\` status before being fully removed. Only Elastic IPs with status \`active\` or \`error\` can be released.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await elasticIpsDeleteElasticIp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
