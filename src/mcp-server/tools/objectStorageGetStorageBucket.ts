/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageGetStorageBucket } from "../../funcs/objectStorageGetStorageBucket.js";
import { GetStorageBucketRequest$zodSchema } from "../../models/getstoragebucketop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetStorageBucketRequest$zodSchema,
};

export const tool$objectStorageGetStorageBucket: ToolDefinition<typeof args> = {
  name: "object-storage-get-storage-bucket",
  description: `Retrieve bucket

Shows details of a specific object storage.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStorageGetStorageBucket(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
