/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualMachineBackupsCreateForVirtualMachine } from "../../funcs/virtualMachineBackupsCreateForVirtualMachine.js";
import { CreateVirtualMachineBackupRequest$zodSchema } from "../../models/createvirtualmachinebackupop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateVirtualMachineBackupRequest$zodSchema,
};

export const tool$virtualMachineBackupsCreateForVirtualMachine: ToolDefinition<
  typeof args
> = {
  name: "virtual-machine-backups-create-for-virtual-machine",
  description: `Create VM backup

Triggers a backup of the given Virtual Machine.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await virtualMachineBackupsCreateForVirtualMachine(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
