/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageGetStorageBucketAccessKeys } from "../../funcs/objectStorageGetStorageBucketAccessKeys.js";
import { GetStorageBucketAccessKeysRequest$zodSchema } from "../../models/getstoragebucketaccesskeysop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetStorageBucketAccessKeysRequest$zodSchema,
};

export const tool$objectStorageGetStorageBucketAccessKeys: ToolDefinition<
  typeof args
> = {
  name: "object-storage-get-storage-bucket-access-keys",
  description: `List bucket access keys

Lists IAM access keys associated with an object storage bucket. Secrets are never returned by this endpoint.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStorageGetStorageBucketAccessKeys(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
