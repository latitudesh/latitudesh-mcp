/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { baselinesPreviewDestroyBaseline } from "../../funcs/baselinesPreviewDestroyBaseline.js";
import { DestroyBaselineRequest$zodSchema } from "../../models/destroybaselineop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DestroyBaselineRequest$zodSchema,
};

export const tool$baselinesPreviewDestroyBaseline: ToolDefinition<typeof args> =
  {
    name: "baselines-preview-destroy-baseline",
    description: `Delete baseline

**Preview.** Available to teams with the \`baselines_api\` feature flag.

Delete a baseline.
`,
    annotations: {
      "title": "",
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await baselinesPreviewDestroyBaseline(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
