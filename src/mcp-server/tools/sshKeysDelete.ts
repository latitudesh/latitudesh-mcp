/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sshKeysDelete } from "../../funcs/sshKeysDelete.js";
import { DeleteSshKeyRequest$zodSchema } from "../../models/deletesshkeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSshKeyRequest$zodSchema,
};

export const tool$sshKeysDelete: ToolDefinition<typeof args> = {
  name: "SSH-keys-delete",
  description: `Delete a SSH Key

Allows you remove SSH Keys in a project. Remove a SSH Key from the project won't revoke the SSH Keys access for previously deploy and reinstall actions.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sshKeysDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
