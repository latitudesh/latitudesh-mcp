/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { firewallsGetAllFirewallAssignments } from "../../funcs/firewallsGetAllFirewallAssignments.js";
import { GetAllFirewallAssignmentsRequest$zodSchema } from "../../models/getallfirewallassignmentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAllFirewallAssignmentsRequest$zodSchema.optional(),
};

export const tool$firewallsGetAllFirewallAssignments: ToolDefinition<
  typeof args
> = {
  name: "firewalls-get-all-firewall-assignments",
  description: `List firewall assignments

Returns a list of all servers assigned to one or more firewalls.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await firewallsGetAllFirewallAssignments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
