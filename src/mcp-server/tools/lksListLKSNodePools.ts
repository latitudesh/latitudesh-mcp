/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lksListLKSNodePools } from "../../funcs/lksListLKSNodePools.js";
import { ListLksNodePoolsRequest$zodSchema } from "../../models/listlksnodepoolsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListLksNodePoolsRequest$zodSchema,
};

export const tool$lksListLKSNodePools: ToolDefinition<typeof args> = {
  name: "lks-list-lks-node-pools",
  description: `List node pools

Lists every node pool of an LKS cluster. The response is not paginated; \`meta.total\` is the number of node pools returned.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await lksListLKSNodePools(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
