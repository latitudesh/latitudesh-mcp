/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userDataRetrieve } from "../../funcs/userDataRetrieve.js";
import { GetUserDataRequest$zodSchema } from "../../models/getuserdataop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetUserDataRequest$zodSchema,
};

export const tool$userDataRetrieve: ToolDefinition<typeof args> = {
  name: "user-data-retrieve",
  description: `Retrieve an User Data

Get User Data in the project. These scripts can be used to configure servers with user data.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userDataRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
