/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversExitRescueMode } from "../../funcs/serversExitRescueMode.js";
import { ServerExitRescueModeRequest$zodSchema } from "../../models/serverexitrescuemodeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ServerExitRescueModeRequest$zodSchema,
};

export const tool$serversExitRescueMode: ToolDefinition<typeof args> = {
  name: "servers-exit-rescue-mode",
  description: `Exits rescue mode for a Server

Exits rescue mode on a given server.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversExitRescueMode(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
