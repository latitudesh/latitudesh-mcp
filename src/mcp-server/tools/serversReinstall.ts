/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversReinstall } from "../../funcs/serversReinstall.js";
import { CreateServerReinstallRequest$zodSchema } from "../../models/createserverreinstallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateServerReinstallRequest$zodSchema,
};

export const tool$serversReinstall: ToolDefinition<typeof args> = {
  name: "servers-reinstall",
  description: `Run Server Reinstall`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversReinstall(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
