/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStoragePostStorageAccessKeys } from "../../funcs/objectStoragePostStorageAccessKeys.js";
import { PostStorageAccessKeysRequest$zodSchema } from "../../models/poststorageaccesskeysop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostStorageAccessKeysRequest$zodSchema,
};

export const tool$objectStoragePostStorageAccessKeys: ToolDefinition<
  typeof args
> = {
  name: "object-storage-post-storage-access-keys",
  description: `Create access key

Creates an object storage IAM access key for a project. The secret is returned only once, in this response, and cannot be retrieved again. The provider is selected by \`storage_class\`: \`standard\` provisions the key on Wasabi and \`high_performance\` provisions it on VAST.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStoragePostStorageAccessKeys(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
