/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStorageMapVolume } from "../../funcs/blockStorageMapVolume.js";
import { MapVolumeRequest$zodSchema } from "../../models/mapvolumeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MapVolumeRequest$zodSchema,
};

export const tool$blockStorageMapVolume: ToolDefinition<typeof args> = {
  name: "block-storage-map-volume",
  description: `Map volume

Maps a high performance volume to a server over NVMe-TCP.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await blockStorageMapVolume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
