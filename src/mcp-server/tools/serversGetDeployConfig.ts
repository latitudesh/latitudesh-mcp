/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversGetDeployConfig } from "../../funcs/serversGetDeployConfig.js";
import { GetServerDeployConfigRequest$zodSchema } from "../../models/getserverdeployconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetServerDeployConfigRequest$zodSchema,
};

export const tool$serversGetDeployConfig: ToolDefinition<typeof args> = {
  name: "servers-get-deploy-config",
  description: `Retrieve Deploy Config`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversGetDeployConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
