/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sshKeysListAll } from "../../funcs/sshKeysListAll.js";
import { GetSshKeysRequest$zodSchema } from "../../models/getsshkeysop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSshKeysRequest$zodSchema.optional(),
};

export const tool$sshKeysListAll: ToolDefinition<typeof args> = {
  name: "SSH-keys-list-all",
  description: `List all SSH Keys

List all SSH Keys in the project. These keys can be used to access servers after deploy and reinstall actions.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sshKeysListAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
