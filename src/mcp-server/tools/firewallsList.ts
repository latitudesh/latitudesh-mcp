/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { firewallsList } from "../../funcs/firewallsList.js";
import { ListFirewallsRequest$zodSchema } from "../../models/listfirewallsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListFirewallsRequest$zodSchema.optional(),
};

export const tool$firewallsList: ToolDefinition<typeof args> = {
  name: "firewalls-list",
  description: `List firewalls

List firewalls`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await firewallsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
