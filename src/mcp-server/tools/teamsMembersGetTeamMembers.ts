/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsMembersGetTeamMembers } from "../../funcs/teamsMembersGetTeamMembers.js";
import { GetTeamMembersRequest$zodSchema } from "../../models/getteammembersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTeamMembersRequest$zodSchema.optional(),
};

export const tool$teamsMembersGetTeamMembers: ToolDefinition<typeof args> = {
  name: "teams-members-get-team-members",
  description: `List all Team Members`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsMembersGetTeamMembers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
