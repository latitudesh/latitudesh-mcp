/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStoragePostStorageBucketLifecycleRules } from "../../funcs/objectStoragePostStorageBucketLifecycleRules.js";
import { PostStorageBucketLifecycleRulesRequest$zodSchema } from "../../models/poststoragebucketlifecyclerulesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostStorageBucketLifecycleRulesRequest$zodSchema,
};

export const tool$objectStoragePostStorageBucketLifecycleRules: ToolDefinition<
  typeof args
> = {
  name: "object-storage-post-storage-bucket-lifecycle-rules",
  description: `Create lifecycle rule

Creates a new lifecycle rule for an object storage bucket. Lifecycle rules automate object expiration based on age.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStoragePostStorageBucketLifecycleRules(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
