/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userDataDeleteProjectUserData } from "../../funcs/userDataDeleteProjectUserData.js";
import { DeleteProjectUserDataRequest$zodSchema } from "../../models/deleteprojectuserdataop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteProjectUserDataRequest$zodSchema,
};

export const tool$userDataDeleteProjectUserData: ToolDefinition<typeof args> = {
  name: "user-data-delete-project-user-data",
  description: `Delete a Project User Data

Allow you remove User Data in a project.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userDataDeleteProjectUserData(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
