/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsSshKeysPostProjectSshKey } from "../../funcs/projectsSshKeysPostProjectSshKey.js";
import { PostProjectSshKeyRequest$zodSchema } from "../../models/postprojectsshkeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostProjectSshKeyRequest$zodSchema,
};

export const tool$projectsSshKeysPostProjectSshKey: ToolDefinition<
  typeof args
> = {
  name: "projects-ssh-keys-post-project-ssh-key",
  description: `Create a Project SSH Key

Allow you create SSH Keys in a project. These keys can be used to access servers after deploy and reinstall actions.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsSshKeysPostProjectSshKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
