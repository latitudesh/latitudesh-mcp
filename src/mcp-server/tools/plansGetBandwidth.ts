/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { plansGetBandwidth } from "../../funcs/plansGetBandwidth.js";
import { GetBandwidthPlansRequest$zodSchema } from "../../models/getbandwidthplansop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetBandwidthPlansRequest$zodSchema.optional(),
};

export const tool$plansGetBandwidth: ToolDefinition<typeof args> = {
  name: "plans-get-bandwidth",
  description: `List all bandwidth plans

Lists all bandwidth plans.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await plansGetBandwidth(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
