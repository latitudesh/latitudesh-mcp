/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kubernetesClustersGetKubernetesCluster } from "../../funcs/kubernetesClustersGetKubernetesCluster.js";
import { GetKubernetesClusterRequest$zodSchema } from "../../models/getkubernetesclusterop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetKubernetesClusterRequest$zodSchema,
};

export const tool$kubernetesClustersGetKubernetesCluster: ToolDefinition<
  typeof args
> = {
  name: "kubernetes-clusters-get-kubernetes-cluster",
  description: `Get a Kubernetes Cluster

Retrieves detailed information about a Kubernetes cluster including its status, control plane, worker node details, and individual node information.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await kubernetesClustersGetKubernetesCluster(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
