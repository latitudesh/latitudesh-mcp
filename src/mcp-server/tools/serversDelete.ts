/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversDelete } from "../../funcs/serversDelete.js";
import { DestroyServerRequest$zodSchema } from "../../models/destroyserverop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DestroyServerRequest$zodSchema,
};

export const tool$serversDelete: ToolDefinition<typeof args> = {
  name: "servers-delete",
  description: `Remove Server`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
