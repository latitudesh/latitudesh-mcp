/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageDeleteStorageAccessKeysUsername } from "../../funcs/objectStorageDeleteStorageAccessKeysUsername.js";
import { DeleteStorageAccessKeysUsernameRequest$zodSchema } from "../../models/deletestorageaccesskeysusernameop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteStorageAccessKeysUsernameRequest$zodSchema,
};

export const tool$objectStorageDeleteStorageAccessKeysUsername: ToolDefinition<
  typeof args
> = {
  name: "object-storage-delete-storage-access-keys-username",
  description: `Delete access key

Permanently deletes an object storage access key and its associated provider-side credentials. Deletion cannot be undone: it revokes the key's credentials and removes the key from the storage provider. For \`standard\` (Wasabi) keys, the IAM user is removed from each of the project's bucket policies, all of the user's access keys are revoked, and the IAM user is deleted. For \`high_performance\` (VAST) keys, the VAST user's S3 keys are revoked, its attached S3 policies are deleted, and the VAST user is removed.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await objectStorageDeleteStorageAccessKeysUsername(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
