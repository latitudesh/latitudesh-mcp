/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userDataUpdateForProject } from "../../funcs/userDataUpdateForProject.js";
import { PutProjectUserDataRequest$zodSchema } from "../../models/putprojectuserdataop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutProjectUserDataRequest$zodSchema,
};

export const tool$userDataUpdateForProject: ToolDefinition<typeof args> = {
  name: "user-data-update-for-project",
  description: `Update a Project User Data

Allow you update User Data in a project.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userDataUpdateForProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
