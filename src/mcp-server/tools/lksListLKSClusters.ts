/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lksListLKSClusters } from "../../funcs/lksListLKSClusters.js";
import { ListLksClustersRequest$zodSchema } from "../../models/listlksclustersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListLksClustersRequest$zodSchema,
};

export const tool$lksListLKSClusters: ToolDefinition<typeof args> = {
  name: "lks-list-lks-clusters",
  description: `List LKS clusters

Lists every LKS cluster of a project. The response is not paginated; \`meta.total\` is the number of clusters returned.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await lksListLKSClusters(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
