/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tagsDelete } from "../../funcs/tagsDelete.js";
import { DestroyTagRequest$zodSchema } from "../../models/destroytagop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DestroyTagRequest$zodSchema,
};

export const tool$tagsDelete: ToolDefinition<typeof args> = {
  name: "tags-delete",
  description: `Delete Tag

Update a Tag in the team.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tagsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
