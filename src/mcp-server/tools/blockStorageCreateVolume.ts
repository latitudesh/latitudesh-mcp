/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStorageCreateVolume } from "../../funcs/blockStorageCreateVolume.js";
import { CreateVolumeRequest$zodSchema } from "../../models/createvolumeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateVolumeRequest$zodSchema,
};

export const tool$blockStorageCreateVolume: ToolDefinition<typeof args> = {
  name: "block-storage-create-volume",
  description: `Create volume

Allows you to add persistent storage to a project. These volumes can be used to store data across your servers.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await blockStorageCreateVolume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
