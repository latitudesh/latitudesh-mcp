/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import { Country, Country$zodSchema } from "./country.js";

export const LksSiteDataType = {
  LksSites: "lks_sites",
} as const;
export type LksSiteDataType = ClosedEnum<typeof LksSiteDataType>;

export const LksSiteDataType$zodSchema = z.enum([
  "lks_sites",
]);

export type LksSiteDataAttributes = {
  name?: string | undefined;
  slug?: string | undefined;
  facility?: string | undefined;
  country?: Country | undefined;
};

export const LksSiteDataAttributes$zodSchema: z.ZodType<LksSiteDataAttributes> =
  z.object({
    country: Country$zodSchema.optional(),
    facility: z.string().optional(),
    name: z.string().optional(),
    slug: z.string().optional(),
  });

export type LksSiteData = {
  id?: string | undefined;
  type?: LksSiteDataType | undefined;
  attributes?: LksSiteDataAttributes | undefined;
};

export const LksSiteData$zodSchema: z.ZodType<LksSiteData> = z.object({
  attributes: z.lazy(() => LksSiteDataAttributes$zodSchema).optional(),
  id: z.string().optional().describe(
    "Site slug — pass it as `site` when creating a cluster",
  ),
  type: LksSiteDataType$zodSchema.optional(),
});
