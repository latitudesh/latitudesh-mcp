/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import { VolumeData, VolumeData$zodSchema } from "./volumedata.js";

export const MapVolumeType2 = {
  Volumes: "volumes",
} as const;
export type MapVolumeType2 = ClosedEnum<typeof MapVolumeType2>;

export const MapVolumeType2$zodSchema = z.enum([
  "volumes",
]);

export type MapVolumeAttributes2 = { server_id: string };

export const MapVolumeAttributes2$zodSchema: z.ZodType<MapVolumeAttributes2> = z
  .object({
    server_id: z.string().describe(
      "ID of the server that will access the volume. The server must belong to the same project and location as the volume, and must be eligible for bonding.",
    ),
  });

export type MapVolumeData2 = {
  type: MapVolumeType2;
  attributes: MapVolumeAttributes2;
};

export const MapVolumeData2$zodSchema: z.ZodType<MapVolumeData2> = z.object({
  attributes: z.lazy(() => MapVolumeAttributes2$zodSchema),
  type: MapVolumeType2$zodSchema,
});

export type MapVolumeRequestBody2 = { data: MapVolumeData2 };

export const MapVolumeRequestBody2$zodSchema: z.ZodType<MapVolumeRequestBody2> =
  z.object({
    data: z.lazy(() => MapVolumeData2$zodSchema),
  });

export type MapVolumeRequest = {
  id: string;
  RequestBody: MapVolumeRequestBody2;
};

export const MapVolumeRequest$zodSchema: z.ZodType<MapVolumeRequest> = z.object(
  {
    RequestBody: z.lazy(() => MapVolumeRequestBody2$zodSchema),
    id: z.string().describe("Volume ID"),
  },
);

export type MapVolumeMeta = {};

export const MapVolumeMeta$zodSchema: z.ZodType<MapVolumeMeta> = z.object({});

/**
 * Accepted
 */
export type MapVolumeResponse = {
  data?: VolumeData | undefined;
  meta?: MapVolumeMeta | undefined;
};

export const MapVolumeResponse$zodSchema: z.ZodType<MapVolumeResponse> = z
  .object({
    data: VolumeData$zodSchema.optional(),
    meta: z.lazy(() => MapVolumeMeta$zodSchema).optional(),
  }).describe("Accepted");
