/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import { ElasticIps, ElasticIps$zodSchema } from "./elasticips.js";

/**
 * The status to filter by
 */
export const FilterStatus = {
  Configuring: "configuring",
  Active: "active",
  Moving: "moving",
  Releasing: "releasing",
  Error: "error",
} as const;
/**
 * The status to filter by
 */
export type FilterStatus = ClosedEnum<typeof FilterStatus>;

export const FilterStatus$zodSchema = z.enum([
  "configuring",
  "active",
  "moving",
  "releasing",
  "error",
]).describe("The status to filter by");

export type ListElasticIpsRequest = {
  filterProject?: string | undefined;
  filterServer?: string | undefined;
  filterStatus?: FilterStatus | undefined;
  pageSize?: number | undefined;
  pageNumber?: number | undefined;
};

export const ListElasticIpsRequest$zodSchema: z.ZodType<ListElasticIpsRequest> =
  z.object({
    filterProject: z.string().describe("The project ID or slug to filter by")
      .optional(),
    filterServer: z.string().describe("The server ID to filter by").optional(),
    filterStatus: FilterStatus$zodSchema.optional().describe(
      "The status to filter by",
    ),
    pageNumber: z.int().default(1).describe(
      "Page number to return (starts at 1)",
    ),
    pageSize: z.int().default(20).describe(
      "Number of items to return per page",
    ),
  });

export type ListElasticIpsResponse = { Result: ElasticIps };

export const ListElasticIpsResponse$zodSchema: z.ZodType<
  ListElasticIpsResponse
> = z.object({
  Result: ElasticIps$zodSchema,
});
