/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type ShowManagedDatabaseMetricsRequest = {
  managed_database_id: string;
  period?: number | undefined;
  queries?: string | undefined;
};

export const ShowManagedDatabaseMetricsRequest$zodSchema: z.ZodType<
  ShowManagedDatabaseMetricsRequest
> = z.object({
  managed_database_id: z.string().describe("Managed database ID"),
  period: z.int().describe(
    "Time window in seconds. One of 1800, 3600, 21600, 86400, 604800 (default 1800).",
  ).optional(),
  queries: z.string().describe(
    "Comma-separated metrics to fetch. Defaults to all: cpuUsage, memoryUsage, tpsUsage, maxConnections, deadlocks, blockedQueries, databaseSize.",
  ).optional(),
});

export type ShowManagedDatabaseMetricsPoint = {
  timestamp: string;
  value: number;
};

export const ShowManagedDatabaseMetricsPoint$zodSchema: z.ZodType<
  ShowManagedDatabaseMetricsPoint
> = z.object({
  timestamp: z.iso.datetime({ offset: true }),
  value: z.number(),
});

export type Metrics = {
  unit: string;
  current: number | null;
  points: Array<ShowManagedDatabaseMetricsPoint>;
};

export const Metrics$zodSchema: z.ZodType<Metrics> = z.object({
  current: z.number().nullable(),
  points: z.array(z.lazy(() => ShowManagedDatabaseMetricsPoint$zodSchema)),
  unit: z.string(),
});

/**
 * OK
 */
export type ShowManagedDatabaseMetricsResponse = {
  from: string;
  to: string;
  metrics: { [k: string]: Metrics };
};

export const ShowManagedDatabaseMetricsResponse$zodSchema: z.ZodType<
  ShowManagedDatabaseMetricsResponse
> = z.object({
  from: z.iso.datetime({ offset: true }),
  metrics: z.record(z.string(), z.lazy(() => Metrics$zodSchema)),
  to: z.iso.datetime({ offset: true }),
}).describe("OK");
