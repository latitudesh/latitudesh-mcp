/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const UpdateKubernetesClusterType = {
  KubernetesClusters: "kubernetes_clusters",
} as const;
export type UpdateKubernetesClusterType = ClosedEnum<
  typeof UpdateKubernetesClusterType
>;

export const UpdateKubernetesClusterType$zodSchema = z.enum([
  "kubernetes_clusters",
]);

/**
 * Provide one of: worker_count, control_plane_count (for scaling), or kubernetes_version (for upgrades). These are mutually exclusive operations.
 */
export type UpdateKubernetesClusterAttributes = {
  worker_count?: number | undefined;
  control_plane_count?: number | undefined;
  worker_plan?: string | null | undefined;
  kubernetes_version?: string | undefined;
};

export const UpdateKubernetesClusterAttributes$zodSchema: z.ZodType<
  UpdateKubernetesClusterAttributes
> = z.object({
  control_plane_count: z.int().optional().describe(
    "Desired number of control plane nodes. Minimum 1. Mutually exclusive with worker_count and kubernetes_version.",
  ),
  kubernetes_version: z.string().optional().describe(
    "Target Kubernetes version for upgrade (e.g., v1.35.0+rke2r1). Mutually exclusive with scaling operations. Must be one minor version higher than current.",
  ),
  worker_count: z.int().optional().describe(
    "Desired number of worker nodes. Must be between 0 and 10. Mutually exclusive with control_plane_count and kubernetes_version.",
  ),
  worker_plan: z.string().nullable().optional().describe(
    "Plan slug for worker nodes. Required when scaling from 0 workers. Ignored when scaling an existing deployment.",
  ),
}).describe(
  "Provide one of: worker_count, control_plane_count (for scaling), or kubernetes_version (for upgrades). These are mutually exclusive operations.",
);

export type UpdateKubernetesClusterData = {
  type: UpdateKubernetesClusterType;
  attributes: UpdateKubernetesClusterAttributes;
};

export const UpdateKubernetesClusterData$zodSchema: z.ZodType<
  UpdateKubernetesClusterData
> = z.object({
  attributes: z.lazy(() => UpdateKubernetesClusterAttributes$zodSchema)
    .describe(
      "Provide one of: worker_count, control_plane_count (for scaling), or kubernetes_version (for upgrades). These are mutually exclusive operations.",
    ),
  type: UpdateKubernetesClusterType$zodSchema,
});

export type UpdateKubernetesCluster = { data: UpdateKubernetesClusterData };

export const UpdateKubernetesCluster$zodSchema: z.ZodType<
  UpdateKubernetesCluster
> = z.object({
  data: z.lazy(() => UpdateKubernetesClusterData$zodSchema),
});
