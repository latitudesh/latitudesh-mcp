/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const Metric = {
  Cpu: "cpu",
  Memory: "memory",
  Network: "network",
  Disk: "disk",
} as const;
export type Metric = ClosedEnum<typeof Metric>;

export const Metric$zodSchema = z.enum([
  "cpu",
  "memory",
  "network",
  "disk",
]);

export const Range = {
  Fivem: "5m",
  Fifteenm: "15m",
  Oneh: "1h",
  TwentyFourh: "24h",
} as const;
export type Range = ClosedEnum<typeof Range>;

export const Range$zodSchema = z.enum([
  "5m",
  "15m",
  "1h",
  "24h",
]);

/**
 * Unit applied to every point value
 */
export const VirtualMachineMetricsUnit = {
  Percent: "percent",
  BytesPerSecond: "bytes_per_second",
} as const;
/**
 * Unit applied to every point value
 */
export type VirtualMachineMetricsUnit = ClosedEnum<
  typeof VirtualMachineMetricsUnit
>;

export const VirtualMachineMetricsUnit$zodSchema = z.enum([
  "percent",
  "bytes_per_second",
]).describe("Unit applied to every point value");

export type Point = {
  timestamp?: string | undefined;
  value?: number | undefined;
};

export const Point$zodSchema: z.ZodType<Point> = z.object({
  timestamp: z.iso.datetime({ offset: true }).optional().describe(
    "ISO 8601 UTC timestamp (seconds precision)",
  ),
  value: z.number().optional().describe(
    "Sampled value, rounded to 2 decimal places",
  ),
});

export type VirtualMachineMetrics = {
  metric?: Metric | undefined;
  range?: Range | undefined;
  step?: string | undefined;
  unit?: VirtualMachineMetricsUnit | undefined;
  points?: Array<Point> | undefined;
};

export const VirtualMachineMetrics$zodSchema: z.ZodType<VirtualMachineMetrics> =
  z.object({
    metric: Metric$zodSchema.optional(),
    points: z.array(z.lazy(() => Point$zodSchema)).optional(),
    range: Range$zodSchema.optional(),
    step: z.string().optional().describe(
      "Sampling interval between adjacent points (e.g. \"15s\", \"1m\")",
    ),
    unit: VirtualMachineMetricsUnit$zodSchema.optional().describe(
      "Unit applied to every point value",
    ),
  });
