/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

/**
 * Backend storage tier of the access key. `standard` targets Wasabi; `high_performance` targets VAST.
 */
export const DeleteStorageAccessKeysUsernameStorageClass = {
  Standard: "standard",
  HighPerformance: "high_performance",
} as const;
/**
 * Backend storage tier of the access key. `standard` targets Wasabi; `high_performance` targets VAST.
 */
export type DeleteStorageAccessKeysUsernameStorageClass = ClosedEnum<
  typeof DeleteStorageAccessKeysUsernameStorageClass
>;

export const DeleteStorageAccessKeysUsernameStorageClass$zodSchema = z.enum([
  "standard",
  "high_performance",
]).describe(
  "Backend storage tier of the access key. `standard` targets Wasabi; `high_performance` targets VAST.",
);

export type DeleteStorageAccessKeysUsernameRequest = {
  username: string;
  storage_class: DeleteStorageAccessKeysUsernameStorageClass;
  project: string;
  region?: string | undefined;
};

export const DeleteStorageAccessKeysUsernameRequest$zodSchema: z.ZodType<
  DeleteStorageAccessKeysUsernameRequest
> = z.object({
  project: z.string().describe("Project ID or slug the access key belongs to."),
  region: z.string().describe(
    "Region slug (e.g., `DAL`, `SAO2`). Required for `high_performance` (VAST) keys to select the VAST cluster; ignored for `standard` (Wasabi) keys.",
  ).optional(),
  storage_class: DeleteStorageAccessKeysUsernameStorageClass$zodSchema.describe(
    "Backend storage tier of the access key. `standard` targets Wasabi; `high_performance` targets VAST.",
  ),
  username: z.string().describe("Name of the access key to delete."),
});
