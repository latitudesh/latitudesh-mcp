/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const KubernetesClusterUpdateResponseType = {
  KubernetesClusters: "kubernetes_clusters",
} as const;
export type KubernetesClusterUpdateResponseType = ClosedEnum<
  typeof KubernetesClusterUpdateResponseType
>;

export const KubernetesClusterUpdateResponseType$zodSchema = z.enum([
  "kubernetes_clusters",
]);

/**
 * The update status. 'scaling' indicates nodes are being added or removed. 'upgrading' indicates a version upgrade is in progress. 'unchanged' indicates no change was needed.
 */
export const KubernetesClusterUpdateResponseStatus = {
  Scaling: "scaling",
  Upgrading: "upgrading",
  Unchanged: "unchanged",
} as const;
/**
 * The update status. 'scaling' indicates nodes are being added or removed. 'upgrading' indicates a version upgrade is in progress. 'unchanged' indicates no change was needed.
 */
export type KubernetesClusterUpdateResponseStatus = ClosedEnum<
  typeof KubernetesClusterUpdateResponseStatus
>;

export const KubernetesClusterUpdateResponseStatus$zodSchema = z.enum([
  "scaling",
  "upgrading",
  "unchanged",
]).describe(
  "The update status. 'scaling' indicates nodes are being added or removed. 'upgrading' indicates a version upgrade is in progress. 'unchanged' indicates no change was needed.",
);

export type KubernetesClusterUpdateResponseAttributes = {
  name?: string | undefined;
  status?: KubernetesClusterUpdateResponseStatus | undefined;
  worker_count?: number | null | undefined;
  control_plane_count?: number | null | undefined;
  kubernetes_version?: string | null | undefined;
};

export const KubernetesClusterUpdateResponseAttributes$zodSchema: z.ZodType<
  KubernetesClusterUpdateResponseAttributes
> = z.object({
  control_plane_count: z.int().nullable().optional().describe(
    "The requested number of control plane nodes. Present when scaling control plane.",
  ),
  kubernetes_version: z.string().nullable().optional().describe(
    "The target Kubernetes version. Present when upgrading version.",
  ),
  name: z.string().optional().describe("The cluster name"),
  status: KubernetesClusterUpdateResponseStatus$zodSchema.optional().describe(
    "The update status. 'scaling' indicates nodes are being added or removed. 'upgrading' indicates a version upgrade is in progress. 'unchanged' indicates no change was needed.",
  ),
  worker_count: z.int().nullable().optional().describe(
    "The requested number of worker nodes. Present when scaling workers.",
  ),
});

export type KubernetesClusterUpdateResponseData = {
  type?: KubernetesClusterUpdateResponseType | undefined;
  id?: string | undefined;
  attributes?: KubernetesClusterUpdateResponseAttributes | undefined;
};

export const KubernetesClusterUpdateResponseData$zodSchema: z.ZodType<
  KubernetesClusterUpdateResponseData
> = z.object({
  attributes: z.lazy(() => KubernetesClusterUpdateResponseAttributes$zodSchema)
    .optional(),
  id: z.string().optional().describe("The cluster ID (format: kc_<hash>)"),
  type: KubernetesClusterUpdateResponseType$zodSchema.optional(),
});

export type KubernetesClusterUpdateResponse = {
  data?: KubernetesClusterUpdateResponseData | undefined;
};

export const KubernetesClusterUpdateResponse$zodSchema: z.ZodType<
  KubernetesClusterUpdateResponse
> = z.object({
  data: z.lazy(() => KubernetesClusterUpdateResponseData$zodSchema).optional(),
});
